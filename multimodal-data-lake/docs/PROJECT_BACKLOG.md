# 项目待办清单

最后更新: 2026-05-07

本文档用于统一记录当前项目的功能补齐、页面重做、能力融合和后续优化项。

后续推进规则:

- 每个待办用唯一编号管理，便于后续增删改查。
- 状态只使用: `TODO` / `DOING` / `DONE` / `DROP`
- 新需求先补到本文档，再开始实现。
- 已完成项不删除，改为 `DONE`，保留实现痕迹。
- 明确废弃或不再做的项，改为 `DROP`，并写原因。

## 当前范围

本轮 backlog 主要来自三部分:

1. 当前仓库 `multimodal-data-lake` 已有功能与缺口
2. `多模态数据湖_prd/BONC_Stitch_ProductSpec_V1.md` 中明确提出的重点页面和能力
3. `Tower-Eye` 中可复用的标注、多模态检索、Agent 编排能力

## 融合原则

后续所有改造遵循以下原则:

1. 现有能力优先保留，不做“为了重做页面而删功能”的处理。
2. 原有图谱能力必须保留，后续只允许做融合、重组、增强，不允许用新页面简单替换掉原有图谱能力。
3. 对外表现是“统一产品”，不是“复制一个新系统再把旧功能丢掉”。
4. 新引入的 PRD 设计、Tower-Eye 能力，默认都要先判断如何与当前项目已有能力融合，再决定是否落地。

## 状态说明

- `TODO`: 已确认要做，尚未开始
- `DOING`: 正在开发
- `DONE`: 已完成并通过当前轮验证
- `DROP`: 暂不做或明确废弃

---

## P0 核心待办

这些是优先影响最终汇报效果和产品完整性的事项，优先级最高。

### BL-001 首页看板角色化重做

- 状态: `DONE`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M6-1 首页看板
  - 当前页面: `frontend/src/pages/DashboardPage.jsx`
- 目标:
  - 将首页从单一技术总览改成角色化看板
  - 运维/DBA/分析师/架构师看到不同重点
  - 强化汇报场景下的第一屏观感
- 预期子项:
  - 总览指标重排
  - 角色入口卡
  - 核心任务/告警/趋势区块重组
  - 与慢 SQL、巡检、数据集、AI 副驾驶形成联动入口

### BL-002 慢 SQL 分析页高保真重做

- 状态: `DONE`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M1-7 慢 SQL 分析
  - 当前相关页面: `frontend/src/pages/mpp/SqlEditorPage.jsx`
- 目标:
  - 做出真正可汇报的慢 SQL 列表、详情、执行计划可视化
  - 保留与 SQL 编辑器的上下文跳转能力
- 预期子项:
  - 慢 SQL 列表页重做
  - 慢 SQL 详情页
  - 执行计划树/节点可视化
  - 与阈值、告警策略联动

### BL-003 自动巡检评分报告页重做

- 状态: `DONE`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M1-12 自动巡检
  - 当前页面: `frontend/src/pages/mpp/InspectionPage.jsx`
- 目标:
  - 从“巡检入口”升级为“评分报告”
  - 展示总分、分类扣分、历史趋势、修复建议
- 预期子项:
  - 健康评分总览
  - 分类结果折叠区
  - 失败项修复建议
  - 历史评分趋势
  - 导出报告能力占位

### BL-004 数据集管理页重做

- 状态: `DONE`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M2-1 数据集管理
  - 当前相关页面: `frontend/src/pages/FilesPage.jsx`
- 目标:
  - 让湖存储从“文件列表”升级为“数据集管理”
  - 对齐数据湖仓定位，而不是纯文件资产页
- 预期子项:
  - 数据集列表页
  - 数据集详情页
  - Schema 展示
  - 向量化配置展示
  - 与版本历史、关联资源联动

### BL-005 数据集版本管理

- 状态: `DONE`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M2-2 数据集版本管理
- 目标:
  - 暴露 Lance Dataset 版本能力
  - 做出版本对比、版本标签、版本回滚的产品界面
- 预期子项:
  - 版本历史列表
  - 版本标签管理
  - 版本对比视图
  - 回滚确认弹窗

### BL-006 AI 数据副驾驶

- 状态: `TODO`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M5-5 AI 数据副驾驶
  - `Tower-Eye/poc/qa/agent.py`
  - `Tower-Eye/poc/qa/nl2sql.py`
- 目标:
  - 建立面向汇报场景的对话查询主入口
  - 支持自然语言问数、检索、推理过程可视化
- 预期子项:
  - 副驾驶对话界面
  - 推理步骤面板
  - SQL / 检索 / 总结链路展示
  - 会话历史与上下文追问

### BL-007 多模态检索工作台升级

- 状态: `TODO`
- 优先级: `P0`
- 参考来源:
  - `多模态数据湖_prd` M5 检索工作台
  - 当前页面: `frontend/src/pages/LakeQueryPage.jsx`
  - `Tower-Eye/poc/search/hybrid_search.py`
- 目标:
  - 将当前检索入口从占位能力升级为真实的多模态检索工作台
  - 支持文搜图、图搜图、混合检索、结构化预过滤
- 预期子项:
  - 文本检索
  - 图片检索
  - 混合检索
  - 结构化过滤
  - 检索策略说明与结果解释

---

## P1 重点待办

这些事项对产品完整度和平台味很重要，排在 P0 之后。

### BL-101 数据治理目录页重做

- 状态: `TODO`
- 优先级: `P1`
- 参考来源:
  - `多模态数据湖_prd` M4 数据治理 / 元数据目录
  - 当前相关页面: `frontend/src/pages/TaskGovernancePage.jsx`
- 目标:
  - 建立统一的数据治理入口
  - 展示元数据目录、版本摘要、血缘、权限

### BL-102 湖查询能力收口

- 状态: `TODO`
- 优先级: `P1`
- 参考来源:
  - 当前页面: `frontend/src/pages/LakeQueryPage.jsx`
  - 近期提交中关于 `湖查询` 的改造
- 目标:
  - 清理当前“已拆入口但能力深度不够”的状态
  - 收口 SQL、向量、多模态、混合、NL2SQL、副驾驶之间的职责边界

### BL-103 接入工作台 / 本地上传 / 工作流 / 任务中心融合收口

- 状态: `TODO`
- 优先级: `P1`
- 参考来源:
  - 当前页面:
    - `frontend/src/pages/IngestionWorkbenchPage.jsx`
    - `frontend/src/pages/UploadPage.jsx`
    - `frontend/src/pages/WorkflowCenterPage.jsx`
    - `frontend/src/pages/TaskCenterPage.jsx`
- 目标:
  - 解决“能力都在，但产品链路还不够统一”的问题
  - 明确来源接入、上传、编排、执行治理的入口关系

### BL-104 湖运维信息架构收口

- 状态: `TODO`
- 优先级: `P1`
- 参考来源:
  - 当前 `mpp` 相关页面
  - `多模态数据湖_prd` M1 模块
- 目标:
  - 将集群、告警、慢 SQL、巡检、日志、审计等运维页形成统一风格和统一导航结构

### BL-105 Ray 作业与任务治理联动增强

- 状态: `TODO`
- 优先级: `P1`
- 参考来源:
  - 当前页面: `frontend/src/pages/RayJobsPage.jsx`
  - 当前页面: `frontend/src/pages/TaskGovernancePage.jsx`
  - `多模态数据湖_prd` M6-3 任务治理
- 目标:
  - 把 Ray 执行态、入湖任务、审计记录统一到一个治理视角下

---

## P2 能力融合与增强

这些事项偏增强、亮点、演示深度扩展，可在核心页面稳定后继续推进。

### BL-201 自动标注能力接入数据湖

- 状态: `TODO`
- 优先级: `P2`
- 参考来源:
  - `Tower-Eye/poc/pipeline/auto_label_engine.py`
  - `Tower-Eye/poc/docs/labeling_workflow.md`
- 目标:
  - 将自动标注能力纳入当前数据湖平台
  - 形成“入湖 -> 标注 -> 审核 -> 训练集”的闭环

### BL-202 标注审核与训练集构建

- 状态: `TODO`
- 优先级: `P2`
- 参考来源:
  - `Tower-Eye` 标注流程
- 目标:
  - 补上人工审核、标注结果回写、训练集导出

### BL-203 检索质量指标与评估看板

- 状态: `TODO`
- 优先级: `P2`
- 参考来源:
  - `Tower-Eye/poc/infra/metrics.py`
- 目标:
  - 对多模态检索质量提供指标统计和评估视图

### BL-204 资产血缘追踪

- 状态: `TODO`
- 优先级: `P2`
- 参考来源:
  - `多模态数据湖_prd` M4-2 数据血缘
- 目标:
  - 展示数据来源、数据集版本、任务链路、服务消费之间的关系

---

## 当前建议推进顺序

建议按以下顺序逐项完成:

1. `BL-001` 首页看板角色化重做
2. `BL-002` 慢 SQL 分析页高保真重做
3. `BL-003` 自动巡检评分报告页重做
4. `BL-004` 数据集管理页重做
5. `BL-005` 数据集版本管理
6. `BL-006` AI 数据副驾驶
7. `BL-007` 多模态检索工作台升级
8. `BL-103` 接入工作台 / 本地上传 / 工作流 / 任务中心融合收口
9. `BL-101` 数据治理目录页重做
10. `BL-201` 自动标注能力接入数据湖

---

## 变更记录

### 2026-05-07

- 创建初版 backlog 文档
- 汇总当前项目、PRD 和 Tower-Eye 三方待办
- 明确 P0 / P1 / P2 优先级
- 启动本轮前三项改造: `BL-001` / `BL-002` / `BL-003`
- 完成 `BL-001` 首页看板角色化重做，加入多角色视角切换与汇报型总览布局
- 完成 `BL-002` 慢 SQL 分析页重做，形成“分析 / 编辑器 / 历史”一体页
- 完成 `BL-003` 自动巡检评分报告页重做，补齐评分、分类、趋势和修复建议
- 验证: `cd frontend && npm run build`
