# DataLink 内网部署需求文档

> 版本：v1.0  
> 日期：2026-09-14  
> 适用场景：企业内网部署，假设内网已具备完整大数据环境（Hadoop、Hive、Spark、Flink、Kafka、ClickHouse、DolphinScheduler 等）

---

## 一、项目概述

### 1.1 项目背景

DataLink 是一款一站式大数据开发治理平台，覆盖数据接入、数据开发、数据治理、数据资产、数据服务等全链路能力。本需求文档旨在明确 DataLink 在企业内网环境中的部署要求，确保平台能够安全、稳定、高效地运行，并与内网现有大数据基础设施无缝集成。

### 1.2 部署目标

| 目标 | 说明 |
|------|------|
| 内网隔离部署 | 所有组件部署在企业内网，不暴露公网访问入口 |
| 复用现有大数据环境 | 对接内网已有的 Hadoop/Hive/Spark/Flink/Kafka/ClickHouse 等集群 |
| 企业级安全 | 集成内网统一认证（LDAP/SSO）、RBAC 权限、操作审计、数据脱敏 |
| 高可用 | 核心组件多实例部署，支持故障自动恢复 |
| 可观测 | 提供监控、日志、告警能力，对接内网监控体系（Prometheus/Grafana） |

### 1.3 适用范围

本文档适用于 DataLink 平台在内网环境的部署规划、实施与验收。

---

## 二、内网大数据环境假设

### 2.1 已具备的大数据组件

| 组件 | 版本要求 | 用途 | 部署形态 |
|------|----------|------|----------|
| Hadoop HDFS | 2.7+ / 3.x | 分布式存储 | 集群（3+ 节点） |
| Hadoop YARN | 2.7+ / 3.x | 资源调度 | 集群 |
| Hive | 2.3+ / 3.x | 数据仓库 | HiveServer2 + Metastore |
| Spark | 2.4+ / 3.x | 批处理计算 | on YARN / on K8s |
| Flink | 1.13+ | 流计算 | on YARN / on K8s |
| Kafka | 2.x / 3.x | 消息队列 | 集群（3+ 节点） |
| ClickHouse | 21.x+ | OLAP 分析 | 集群 |
| MySQL | 5.7+ / 8.0 | 元数据库 | 主从 |
| Redis | 5.x+ | 缓存/会话 | 集群或主从 |
| DolphinScheduler | 2.x / 3.x | 任务调度 | 集群 |
| Nginx | 1.18+ | 反向代理/负载均衡 | - |

### 2.2 网络拓扑假设

```
┌─────────────────────────────────────────────────────────────┐
│                       企业内网                               │
│                                                              │
│  ┌──────────┐   ┌──────────────┐   ┌───────────────────┐   │
│  │  用户终端 │──▶│  Nginx (LB)  │──▶│  DataLink 应用集群 │   │
│  └──────────┘   └──────────────┘   └───────────────────┘   │
│                                       │        │            │
│                         ┌─────────────┘        └──────┐     │
│                         ▼                              ▼     │
│                  ┌──────────────┐              ┌──────────┐ │
│                  │ MySQL (主从) │              │  Redis   │ │
│                  └──────────────┘              └──────────┘ │
│                         │                                    │
│          ┌──────────────┼──────────────┬──────────────┐     │
│          ▼              ▼              ▼              ▼     │
│   ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌─────────┐ │
│   │ Hadoop     │ │   Hive     │ │  Flink     │ │  Kafka  │ │
│   │ (HDFS/YARN)│ │ (HS2/MS)   │ │ (on YARN)  │ │ (集群)  │ │
│   └────────────┘ └────────────┘ └────────────┘ └─────────┘ │
│          │              │              │              │     │
│          └──────────────┴──────────────┴──────────────┘     │
│                         │                                    │
│                         ▼                                    │
│                  ┌──────────────┐                            │
│                  │ ClickHouse   │                            │
│                  └──────────────┘                            │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           运维监控区（Prometheus / Grafana / ELK）      │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、功能需求

### 3.1 数据接入

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-01 | 支持 MySQL/PostgreSQL/Oracle/ClickHouse/Hive 数据源接入 | 高 | 提供数据源管理界面，支持测试连接 |
| F-02 | 支持基于 DataX 的离线数据同步 | 高 | 可视化配置同步任务，支持全量/增量 |
| F-03 | 支持基于 Flink CDC 的实时数据同步 | 中 | MySQL Binlog → Kafka → Hive/ClickHouse |
| F-04 | 支持 CSV 文件导入建表 | 中 | 上传 CSV 自动建表并导入数据 |
| F-05 | 支持数据源分组管理 | 中 | 按业务线/团队对数据源分组 |

### 3.2 数据开发

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-10 | 在线 SQL 编辑器（Hive/Spark SQL） | 高 | 支持语法高亮、自动补全、格式化 |
| F-11 | SQL 执行与结果预览 | 高 | 支持分页、异步执行、导出 CSV |
| F-12 | 脚本开发与版本管理 | 高 | 支持脚本保存、版本回滚 |
| F-13 | 实时开发（Flink SQL） | 中 | 支持 Flink SQL 编写、提交、状态管理 |
| F-14 | AI 辅助开发 | 中 | NL2SQL、SQL 解释、SQL 优化建议 |
| F-15 | SQL 片段/模板库 | 中 | 可复用 SQL 模板，支持变量渲染 |
| F-16 | SQL 查询历史与收藏 | 低 | 历史记录查询、常用 SQL 收藏 |

### 3.3 数据治理

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-20 | 数据标准管理 | 高 | 数据域、业务过程、词根、修饰词、时间周期 |
| F-21 | 数据建模 | 高 | 维度表、事实表、汇总表设计与发布 |
| F-22 | 数据质量监控 | 高 | 质量规则定义、定时校验、异常告警 |
| F-23 | 数据血缘 | 中 | 表级/字段级血缘追溯与可视化 |
| F-24 | 数据生命周期管理 | 中 | 冷热分层、归档、过期清理 |
| F-25 | 元数据自动采集 | 中 | 扫描数据源，自动同步元数据到资产目录 |
| F-26 | 数据字典管理 | 中 | 字典类型与字典项维护 |

### 3.4 数据资产

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-30 | 资产目录 | 高 | 资产分级、标签、负责人、热度、收藏 |
| F-31 | 全局数据搜索 | 高 | 跨资产、表、字段、数据源、脚本统一搜索 |
| F-32 | 数据预览 | 中 | 表数据采样预览、表结构统计 |

### 3.5 数据服务

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-40 | SQL 发布为 REST API | 高 | 将查询发布为 HTTP 接口，支持鉴权 |
| F-41 | API 调用统计与限流 | 中 | 调用量统计、滑动窗口限流 |
| F-42 | 数据脱敏 | 中 | 手机号、身份证、邮箱、银行卡等脱敏 |

### 3.6 权限与安全

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-50 | RBAC 权限模型 | 高 | 用户-角色-权限，支持菜单/按钮/数据级权限 |
| F-51 | 行级/列级数据权限 | 高 | 隐藏列、脱敏列、行过滤条件 |
| F-52 | 操作审计日志 | 高 | 记录所有关键操作，支持追溯 |
| F-53 | SQL 审批流 | 中 | 危险 SQL 需审批后执行 |
| F-54 | 集成内网统一认证 | 中 | LDAP / SSO 对接 |

### 3.7 系统管理

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-60 | 数据源管理（新建/编辑/删除/测试） | 高 | 系统管理下的数据源管理 |
| F-61 | 项目空间管理 | 高 | 项目创建、成员管理、资源配额 |
| F-62 | 用户与角色管理 | 高 | 用户增删改、角色分配 |
| F-63 | AI 配置 | 中 | API Key、模型、Base URL 配置（加密存储） |
| F-64 | 告警通知配置 | 中 | 钉钉/企微/邮件/Webhook |
| F-65 | 平台监控大盘 | 中 | 数据源状态、质量趋势、API 调用趋势 |

---

## 四、非功能需求

### 4.1 性能需求

| 指标 | 要求 |
|------|------|
| 页面响应时间 | 普通页面 ≤ 2s，列表查询 ≤ 3s |
| SQL 执行 | 支持异步执行，超时可配置（默认 300s） |
| 并发用户 | 支持 ≥ 100 并发用户在线 |
| API 响应 | 数据服务 API P95 ≤ 1s（缓存命中） |
| 数据同步 | 单表同步吞吐量 ≥ 10 MB/s（局域网） |

### 4.2 可用性需求

| 指标 | 要求 |
|------|------|
| 系统可用性 | ≥ 99.5%（月度） |
| 应用多实例 | DataLink 应用 ≥ 2 实例，Nginx 负载均衡 |
| 数据库高可用 | MySQL 主从复制，主故障自动切换 |
| 故障恢复 | 应用实例故障自动拉起（K8s/Supervisor） |
| 数据备份 | MySQL 每日全量备份 + binlog 增量备份 |

### 4.3 安全需求

| 类别 | 要求 |
|------|------|
| 传输加密 | 全链路 HTTPS（内网可使用自签名证书） |
| 存储加密 | 敏感信息（数据库密码、API Key）AES 加密存储 |
| 认证 | 强制登录认证，支持 LDAP/SSO 集成 |
| 授权 | RBAC 权限控制，最小权限原则 |
| 审计 | 关键操作全量审计日志，保留 ≥ 180 天 |
| 网络隔离 | 应用区与数据区网络隔离，最小开放端口 |
| 漏洞防护 | 定期扫描依赖漏洞，及时修复 |

### 4.4 可扩展性需求

| 类别 | 要求 |
|------|------|
| 水平扩展 | 应用无状态，可通过增加实例水平扩展 |
| 数据源扩展 | 支持新增数据源类型（插件化） |
| 存储扩展 | HDFS/ClickHouse 可独立扩容 |
| 调度扩展 | 对接 DolphinScheduler 调度集群 |

---

## 五、部署架构要求

### 5.1 部署模式

采用 **Docker Compose** 或 **Kubernetes** 部署，推荐生产使用 K8s。

### 5.2 节点规划（最低配置）

| 节点 | 角色 | 配置 | 部署组件 |
|------|------|------|----------|
| node-1 | 应用+管理 | 8C16G | DataLink 应用、Nginx、MySQL 主、Redis |
| node-2 | 应用 | 8C16G | DataLink 应用、MySQL 从 |
| node-3 | 计算接入 | 16C32G | DataX Worker、Flink Gateway |

> 大数据组件（Hadoop/Hive/Flink/Kafka/ClickHouse）复用现有集群，不占用 DataLink 节点资源。

### 5.3 存储规划

| 用途 | 存储类型 | 容量 | 说明 |
|------|----------|------|------|
| MySQL 数据 | SSD | ≥ 100 GB | 元数据、日志 |
| 应用日志 | HDD | ≥ 50 GB | 按 7 天滚动 |
| 临时文件 | 本地盘 | ≥ 20 GB | CSV 导入导出、临时结果 |
| HDFS 存储 | 复用现有 | - | 数据表、归档数据 |

### 5.4 端口规划

| 端口 | 服务 | 访问范围 |
|------|------|----------|
| 80/443 | Nginx 反向代理 | 用户区 → 应用区 |
| 8099 | DataLink 应用 | 应用区内（Nginx 转发） |
| 3306 | MySQL | 应用区 → 数据区 |
| 6379 | Redis | 应用区内 |
| 10000 | HiveServer2 | 应用区 → 大数据区 |
| 9083 | Hive Metastore | 应用区 → 大数据区 |
| 9092 | Kafka Broker | 应用区 → 大数据区 |
| 8123 | ClickHouse HTTP | 应用区 → 大数据区 |
| 8081 | Flink REST | 应用区 → 大数据区 |

---

## 六、集成需求

### 6.1 与大数据集群集成

| 集成项 | 集成方式 | 要求 |
|--------|----------|------|
| HDFS | 配置 core-site.xml / hdfs-site.xml | 应用节点可访问 NameNode |
| Hive | JDBC 连接 HiveServer2 | 配置 Hive 客户端，支持 Kerberos（可选） |
| Spark | spark-submit / Livy REST API | 提交 Spark SQL 任务到 YARN |
| Flink | Flink REST API / YARN | 提交 Flink SQL 作业 |
| Kafka | Kafka 客户端 | 读写 Topic，支持 SASL（可选） |
| ClickHouse | JDBC / HTTP | 读写 ClickHouse 表 |
| DolphinScheduler | REST API | 调度任务下发与状态同步 |

### 6.2 与企业安全体系集成

| 集成项 | 集成方式 | 要求 |
|--------|----------|------|
| 统一认证 | LDAP / OAuth2 / SAML | 对接内网 AD/LDAP 或 SSO |
| 权限同步 | LDAP 组映射角色 | 定期同步组织结构 |
| 审计日志 | Syslog / Kafka | 审计日志推送至内网日志平台 |
| 告警通知 | 钉钉/企微/邮件/Webhook | 对接内网告警渠道 |
| 监控指标 | Prometheus 拉取 | 暴露 JVM、业务指标端点 |

### 6.3 与运维监控集成

| 集成项 | 集成方式 | 要求 |
|--------|----------|------|
| 健康检查 | /actuator/health | 负载均衡探活 |
| 指标暴露 | /actuator/prometheus | Prometheus 采集 |
| 日志收集 | Filebeat → ELK | 应用日志统一收集 |
| 告警规则 | Prometheus Alertmanager | 对接内网告警平台 |

---

## 七、数据安全与合规

### 7.1 数据分级

| 级别 | 说明 | 处理要求 |
|------|------|----------|
| L1 公开 | 可对外公开的数据 | 无特殊限制 |
| L2 内部 | 企业内部数据 | 内网访问，需授权 |
| L3 敏感 | 涉及个人信息/商业秘密 | 需脱敏、加密、审计 |
| L4 机密 | 核心机密数据 | 严格管控，专人审批 |

### 7.2 数据脱敏规则

| 字段类型 | 脱敏规则 | 示例 |
|----------|----------|------|
| 手机号 | 中间 4 位掩码 | 138****1234 |
| 身份证 | 中间 10 位掩码 | 110101********1234 |
| 邮箱 | 用户名部分掩码 | te***@example.com |
| 银行卡 | 仅保留后 4 位 | ****1234 |
| 姓名 | 保留姓，名掩码 | 张* |

### 7.3 数据生命周期

| 阶段 | 策略 |
|------|------|
| 热数据（90 天内） | 存 SSD/内存，高频访问 |
| 温数据（90 天 ~ 1 年） | 存 HDD，定期访问 |
| 冷数据（1 年以上） | 归档至 HDFS，压缩存储 |
| 过期数据 | 按策略自动删除，需审批确认 |

---

## 八、运维要求

### 8.1 部署要求

- 提供一键部署脚本（`setup-datalink.sh`）
- 提供 Docker Compose 编排文件
- 提供 K8s Helm Chart（可选）
- 配置参数全部通过环境变量或配置文件注入，不硬编码

### 8.2 升级要求

- 支持滚动升级，不停机
- 提供数据库迁移脚本（Flyway/Liquibase）
- 支持版本回滚
- 升级前自动备份数据库

### 8.3 监控要求

| 监控项 | 指标 | 告警阈值 |
|--------|------|----------|
| 应用健康 | 实例存活数 | < 期望实例数 |
| JVM | 堆内存使用率 | > 80% |
| 数据库 | 连接池使用率 | > 85% |
| 接口 | 响应时间 P95 | > 3s |
| 接口 | 错误率 | > 1% |
| 磁盘 | 日志目录使用率 | > 80% |

### 8.4 备份要求

| 备份对象 | 频率 | 保留 | 方式 |
|----------|------|------|------|
| MySQL 全量 | 每日 | 30 天 | mysqldump |
| MySQL binlog | 实时 | 7 天 | 增量 |
| 配置文件 | 变更时 | 永久 | Git 版本控制 |

---

## 九、验收标准

### 9.1 功能验收

- 所有高优先级功能（F-01 ~ F-65 中标记"高"）通过测试用例
- 核心流程（数据源接入 → SQL 开发 → 发布 → 数据服务）端到端跑通
- 权限控制验证：不同角色登录可见菜单/数据不同

### 9.2 性能验收

- 100 并发用户压测，接口 P95 ≤ 3s，错误率 ≤ 0.1%
- SQL 查询 1 亿级数据表，响应时间 ≤ 60s（依赖大数据集群性能）
- 数据同步 10GB 数据，耗时 ≤ 20 分钟

### 9.3 安全验收

- 未登录访问 API 返回 401
- 越权访问返回 403
- 敏感信息（密码、API Key）在数据库中加密存储
- 审计日志完整记录关键操作

### 9.4 可用性验收

- 模拟单应用实例故障，服务不中断
- 模拟 MySQL 主库故障，从库自动切换
- 大数据集群单节点故障，DataLink 功能不受影响

---

## 十、交付物清单

| 交付物 | 说明 |
|--------|------|
| DataLink 部署包 | JAR 包 + Docker 镜像 + 配置模板 |
| 部署文档 | 本需求文档 + ENTERPRISE_DEPLOY.md 部署指南 |
| 数据库初始化脚本 | `sql/` 目录下所有 SQL 脚本 |
| 部署脚本 | `setup-datalink.sh` 一键部署脚本 |
| Docker Compose | `docker-compose.yml` 编排文件 |
| 运维手册 | 日常运维、故障排查、扩容升级指引 |
| 验收测试报告 | 功能/性能/安全/可用性测试报告 |

---

## 附录 A：环境变量清单

| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| `DB_HOST` | MySQL 主机 | 127.0.0.1 | 否 |
| `DB_PORT` | MySQL 端口 | 3307 | 否 |
| `DB_USERNAME` | MySQL 用户名 | root | 否 |
| `DB_PASSWORD` | MySQL 密码 | 空 | 是 |
| `DB_NAME` | 数据库名 | datalink | 否 |
| `CRYPTO_KEY` | 加密密钥（16/24/32 字节） | 空 | 是 |
| `REDIS_HOST` | Redis 主机 | 127.0.0.1 | 否 |
| `REDIS_PORT` | Redis 端口 | 6379 | 否 |
| `CORS_ALLOWED_ORIGINS` | 允许跨域来源 | * | 否 |
| `SPRINGDOC_ENABLED` | 是否开启接口文档 | true | 否 |
| `LOG_FILE_MAX_SIZE` | 日志单文件大小 | 100MB | 否 |
| `LOG_FILE_MAX_HISTORY` | 日志保留天数 | 30 | 否 |

## 附录 B：数据库初始化

按序号执行 `sql/` 目录下的脚本：

```bash
# 1. 基础表结构
mysql -uroot -p datalink < sql/00_init.sql
mysql -uroot -p datalink < sql/24_dataphin_standard.sql
mysql -uroot -p datalink < sql/25_dataphin_modeling.sql

# 2. 扩展功能表
for f in sql/2[6-9]_*.sql sql/3[0-9]_*.sql sql/4[0-5]_*.sql; do
  mysql -uroot -p datalink < $f
done
```

> 具体脚本以项目 `sql/` 目录实际文件为准。